import fetch from 'node-fetch';

const sendTelegramMessage = async (message) => {
  // Implement your logic to send messages to Telegram
  // console.log("Telegram ", message);
};

export { sendTelegramMessage };